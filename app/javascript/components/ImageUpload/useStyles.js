import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  crop: {
    maxHeight: 300,
    maxWidth: 300,
    marginBottom: 10,
  },
}));

export default useStyles;
